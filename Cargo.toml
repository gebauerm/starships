[package]
name = "starships"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy= "=0.19.0"
bevy_dev_tools = "0.19.0"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level=1

[profile.dev.package."*"]
opt-level = 3
